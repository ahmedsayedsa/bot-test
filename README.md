WhatsApp bot package.
- Edit environment variables in your deployment (do NOT commit secrets).
- Start: `npm install` then `node index.js`.
- export_email.py can be scheduled via cron/Cloud Scheduler; it needs EMAIL_* env vars.
